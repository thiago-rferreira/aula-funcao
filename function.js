// Como eu uso functions??
// Tenho que declarar!!!!!!
// Como declarar no js?

// function nomeDaFuncao(params) {
//     //corpo (bloco de codigo)
//     return resultado;
// } 

// Exemplo real

function soma(valor1, valor2) {
    //bloco de codigo
    let resultado = 0;

    // Aqui eu faço a soma
    resultado = valor1 + valor2;

    // Exibo
    console.log(resultado);

    // Faço o return
    return resultado;
}

soma(1,3);
soma(6,8);
soma(9,9);
soma(100,999);
soma(-999,90);